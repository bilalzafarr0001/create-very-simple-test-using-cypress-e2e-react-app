describe("empty spec", () => {
  it("is working", () => {
    expect(true).to.equal(true);
  });

  it("passes", () => {
    // eslint-disable-next-line no-undef
    cy.visit("https://example.cypress.io");
  });
});



